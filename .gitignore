.env
.env.*
!.env.example
venv/
.venv/
CLAUDE.md
OFFICIAL_TASK.md
PROJECT_CONTEXT.md
# All build artifacts and downloaded models. Regenerated by the scripts in
# scripts/ (see README "Reproducing from scratch"); ~4 GB in total, so none of
# it belongs in git. Measurement outputs live in results/ and ARE committed.
data/
.cache/
.cache/huggingface/
*.safetensors
*.bin
model_cache/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
.idea/
.vscode/
.claude

# scratch / local-only
test.py
cloudflared
NEXT_SESSION.md
*.log

# rag-local-eval-loop (the organizers' harness, copied in per its runbook).
# Their tooling, not ours - graders copy their own fresh clone in. Our side of
# the contract is app/embedder.py + app/generator.py + app/config.py, which ARE
# committed. .venv is the venv their launcher looks for; ours is venv/.
eval/
run.ps1
run.sh
.venv/
results/2026*.json
task2_hhg.txt